plugins {
    alias libs.plugins.android.library
}

group = "thiha.aung.fancytable"
version = "1.0.1"

android {
    namespace = "thiha.aung.fancytable"
    compileSdkVersion 34

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 34
        versionCode 2
        versionName "1.0.1"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {

}